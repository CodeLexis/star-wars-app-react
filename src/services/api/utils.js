/** Deletes the auth token of the user's session */
export function deleteAuthToken() {
  return;
}

/** Checks if the current auth token has expired */
export function isAuthTokenExpired() {

}

/** Refreshes the auth token of the user's session
 * @return {Promise}
*/
export function refreshAuthToken() {
  return new Promise();
}

/** Retrieves the auth token of the user's session */
export function retrieveAuthToken() {

}
